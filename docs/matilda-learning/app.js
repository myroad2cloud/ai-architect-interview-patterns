const lessons=[
{title:'The assessment mental model',time:'8 min',intro:'A migration assessment is a decision process. Tools collect evidence; people confirm what the evidence means.',analogy:'Imagine moving a hospital while it remains open. A list of rooms tells you what exists. The dependency map shows why the emergency ward needs the lab, pharmacy, identity system and network at the same time.',sections:[['What Matilda does','Matilda Cloud describes a suite spanning discovery and assessment, migration and optimization. Discover is positioned around application-centric visibility, dependencies, suitability, modernization options and wave planning. Exact screens, connectors, collection depth and reports depend on the product, version and permissions in your engagement.'],['What the architect owns','Define the scope and confidence level. Ask which assets are in the migration boundary, which applications they run, and which dependencies cross that boundary. A tool recommendation is evidence to test. It is not a signed architecture decision.'],['How Azure Migrate fits','Azure Migrate provides Microsoft’s discovery and assessment path for Azure. It can discover servers, application components and dependencies, then assess Azure readiness, sizing and cost. The overlap with Matilda is meaningful. Use the outputs together only where each answers a clear question.']],exercise:'Write one assessment question each for an infrastructure lead, application owner and CISO. For example: “Which service calls this SQL instance during month-end close?”',takeaway:'The output is a validated application view and a migration decision record, not just a server list.'},
{title:'Scope and discovery design',time:'12 min',intro:'Before a collector runs, decide what is being counted, how it will be reached, and what “complete” means.',analogy:'A census with no city boundary produces a confident-looking number for the wrong population.',sections:[['Define the boundary','Record datacenters, vCenters, subscriptions, networks, environments, OS families and excluded assets. Decide whether databases, Kubernetes, PaaS and shadow IT are in scope. Record the source of truth for each class.'],['Plan collection','Select the documented Matilda deployment and access method for the relevant platform. Confirm service accounts, least privilege, outbound connectivity, firewalls, DNS and proxy behavior. Obtain the customer’s security approval before granting access. Do not assume agentless means accessless.'],['Create a coverage ledger','For every source, capture expected count, discovered count, exclusions, error count, last successful collection and owner. Keep the raw inventory separate from the agreed in-scope baseline. A powered-off VM, duplicate CMDB row and unreachable DMZ machine need different treatment.']],exercise:'Make a four-column table: source, expected assets, discovered assets, reason for the difference. Start with vCenter, CMDB and Matilda.',takeaway:'Discovery completeness is a measured finding, not a status label.'},
{title:'Deep discovery and security',time:'11 min',intro:'Deep discovery can reveal software, processes and database metadata. It also raises specific security and privacy questions.',analogy:'Opening the cupboards helps plan the move. You still need the owner’s key and permission to inspect them.',sections:[['Inventory versus depth','Infrastructure discovery may show CPU, memory, disks and OS. Deeper discovery may show services, processes, installed software, database instances or schemas. Confirm the supported collection path in the Matilda documentation for your source platform. Missing privileges can produce a partial map that looks complete.'],['Security review','Record exactly which account accesses which systems, which ports are needed, what data is collected and transmitted, where it is stored, how long it stays, and who can see it. Validate encryption, residency and contract terms. Never promise that application data is excluded without checking the selected method and scope.'],['Network view','Draw the collector, management endpoints, target servers and SaaS destination. Include outbound proxy, DNS resolution, authentication path, segmentation and firewall logging. In a restricted zone, agree a separate collection method or disclose the gap.']],exercise:'Draft the CISO preflight: account scope, firewall flows, metadata fields, retention, residency and an offboarding plan.',takeaway:'Quality and security depend on the same thing: a documented, approved discovery architecture.'},
{title:'Dependencies and application groups',time:'14 min',intro:'Observed connections suggest relationships. Workshops turn them into an application model.',analogy:'A refrigerator uses power and water, but a technician still needs to know which valve is shared with the rest of the building.',sections:[['Read a dependency graph','Start with a business-facing entry point. Trace inbound callers and outbound connections to app tiers, databases, identity, files, queues, APIs and shared services. Review ports, direction, time window and recurring patterns. A short observation period can miss payroll, month-end or disaster recovery paths.'],['Group deliberately','Create an application group with a named owner, service purpose, environment, criticality and component roles. Mark shared servers clearly: a SQL cluster serving six applications cannot be “owned” by only one group. Flag unknown nodes for review rather than silently assigning them.'],['Validate with people','Ask owners about batch schedules, vendor integrations, hard-coded IPs, DNS, certificates, firewall rules and data flows. Compare Matilda’s graph with CMDB, Azure Migrate dependency data, logs and architecture diagrams. Record confirmed, probable and unknown relationships.']],exercise:'Use the Dependency lab. Select the four servers that form the portal, then identify the shared identity service and external payment API.',takeaway:'Traffic is evidence of communication. Business owners validate service meaning and migration boundaries.'},
{title:'Assess Azure target options',time:'13 min',intro:'Rehost, replatform and rearchitect are choices about change, risk and value. No single option is right for every workload.',analogy:'When moving a hospital, you can relocate the existing equipment, replace a piece with a managed service, or redesign the workflow. Each choice changes time, training and operating risk.',sections:[['Rehost','Move the current architecture mainly as Azure VMs. It can reduce application change but carries existing technical debt and operating tasks. Verify OS support, drivers, latency, licensing and target landing zone controls.'],['Replatform','Change a layer, such as IIS to App Service or SQL Server to Azure SQL Managed Instance, while retaining much of the application. Validate feature compatibility, authentication, networking, backup, HA/DR and performance. A plausible mapping is not a migration approval.'],['Rearchitect','Redesign components for new behavior, scale or resilience. This can bring value but requires code ownership, testing capacity and a business case. Keep it as a separate funded workstream when the assessment window cannot prove feasibility.'],['Decision record','For each application, record the options considered, evidence, owner, cost basis, risks, dependencies and next validation task. Map recommendations to the organization’s landing zone, security baseline and operating model.']],exercise:'For a legacy IIS + SQL workload, write one reason to rehost first and one condition that would make replatforming worth testing.',takeaway:'Choose an option with a defensible trade-off, not because a tool ranked it first.'},
{title:'Rightsizing and TCO',time:'12 min',intro:'A source VM size is a starting point. An Azure estimate needs utilization, resilience and commercial assumptions.',analogy:'A six-bedroom house does not prove you need six hotel rooms. But one quiet Tuesday does not prove you only need one.',sections:[['Read performance carefully','Check the observation period, CPU percentiles, memory pressure, IOPS, throughput, latency, peak schedules and growth. Separate idle test workloads from business-critical production. Capture coverage gaps and measurement confidence.'],['Build comparable costs','Specify Azure region, target SKU, storage, licensing, reserved capacity or savings plan assumptions, network egress, backup, monitoring and support. Include HA/DR and non-production patterns where required. Compare like for like with current cost, including facilities and operations where reliable.'],['Show sensitivity','Give at least a base case and a higher-demand case for uncertain workloads. Rightsizing is reversible after migration, but an underpowered initial target can cause an avoidable incident. Note where an Azure Migrate estimate and a Matilda estimate use different inputs.']],exercise:'A 16-vCPU VM averages 12% CPU but spikes at month-end. List three more measurements you need before proposing a smaller Azure SKU.',takeaway:'The cost model is only as strong as its workload window and stated assumptions.'},
{title:'Migration waves and cutover',time:'13 min',intro:'Waves are coordinated business changes, not arbitrary blocks of servers.',analogy:'You would move the hospital lab and the ward it supports as a planned operation, with a temporary path for patients during transition.',sections:[['Construct the wave','Group applications by dependency, complexity, criticality and target readiness. Consider shared services, network connectivity, identity, data replication, vendor support and change windows. Pick a low-risk pilot that tests the migration factory without hiding hard dependencies.'],['Plan cutover','Document order, data sync, DNS and firewall changes, smoke tests, rollback trigger and who authorizes go/no-go. Check cross-wave latency when an app remains on-premises while a database moves to Azure.'],['Challenge automation','Matilda advertises wave planning capabilities. Treat a generated sequence as a draft. A shared SQL cluster, unsupported OS or quarter-end blackout can override it. Make owners sign off on the application and its cutover plan.']],exercise:'Open the Wave planner. Choose which group moves first, and explain the dependency or risk that drives your choice.',takeaway:'A wave is complete when the business service works and the operations team can support it.'},
{title:'Deliver a client-ready assessment',time:'10 min',intro:'A good assessment makes the next investment and migration decisions clear.',analogy:'A detective does not hand over a pile of photographs. They explain what each photo proves, what remains uncertain and which lead to investigate next.',sections:[['Core deliverables','Provide a reconciled inventory, application and dependency map, scoped exclusions, Azure target options, sizing and cost assumptions, risk register, migration wave plan and decisions pending. Label all estimates by data source and collection window.'],['Executive and technical views','Executives need choices, cost range, risk and timeline conditions. Engineers need asset IDs, network flows, target design, prerequisites, ownership and test criteria. Keep both views traceable to the same baseline.'],['Two-week assessment reality','A two-week window may support a directional assessment if access and owner workshops are ready. It rarely proves every dependency or detailed modernization feasibility for a large estate. State the confidence level and paid follow-on discovery needed to close gaps.']],exercise:'Write a one-page readout with: assessed scope, confidence, three decisions, three gaps and the next workshop owner.',takeaway:'Make findings usable, with enough evidence to challenge each recommendation.'}
];
const questions=[{q:'Matilda finds 591 assets; vCenter lists 607 and CMDB lists 620. What is the first architect action?',choices:['Use 591 as final because Matilda discovered it','Reconcile the counts and classify missing or duplicate assets','Average all three numbers'],answer:1,why:'A coverage ledger explains which source is current, which assets were excluded, and where collection failed.'},{q:'A dependency graph shows APP01 calling SQL01. What can you conclude?',choices:['They communicate in the observation window','They must migrate in one wave','SQL01 belongs exclusively to APP01'],answer:0,why:'The traffic is evidence of communication. Ownership and migration grouping need further validation.'},{q:'Which input can invalidate a rightsizing recommendation based on average CPU?',choices:['A readable VM name','Month-end peaks and memory pressure','The color of the source dashboard'],answer:1,why:'Peak demand, memory, I/O and the collection window are material to sizing.'},{q:'Who approves the final application grouping?',choices:['The tool alone','The architect with application and platform owners','Only the procurement team'],answer:1,why:'Owners provide business context, while architects reconcile observed technical data.'},{q:'Which statement about agentless discovery is safest?',choices:['It needs no credentials or security review','It always captures application data','It still needs authorized access and agreed network flows'],answer:2,why:'Agentless describes the collection approach. Access and data-handling controls still matter.'}];
const checklist=['Scope, exclusions and source-of-truth owners agreed','Discovery access and security review approved','Expected and discovered asset counts reconciled','Application ownership and criticality confirmed','Dependency observation window and unknown links recorded','Azure target options and compatibility checks documented','Performance window, sizing and cost assumptions stated','Landing zone, network, identity and security prerequisites checked','Wave plan reviewed with owners and migration leads','Open decisions, confidence level and next actions published'];
const saved=new Set(JSON.parse(localStorage.getItem('matildaDone')||'[]'));let view='home',qIndex=0,score=0,answered=false,selectedNodes=new Set();const $=s=>document.querySelector(s);function esc(x){return String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));}function nav(){let h='<button class="navbtn '+(view==='home'?'active':'')+'" data-view="home">⌂ <span>Overview</span></button>';lessons.forEach((l,i)=>h+=`<button class="navbtn ${view==='lesson'+i?'active':''}" data-view="lesson${i}"><span class="num">${saved.has(i)?'✓':String(i+1).padStart(2,'0')}</span><span>${esc(l.title)}</span></button>`);$('#lessonNav').innerHTML=h;document.querySelectorAll('.navbtn[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===view));$('#progressText').textContent=`${saved.size} / ${lessons.length}`;$('#progressBar').style.width=(saved.size/lessons.length*100)+'%';}function setView(v){view=v;nav();let c=$('#content');c.innerHTML=render();c.focus();window.scrollTo(0,0);$('.sidebar').classList.remove('open');$('#menuBtn').setAttribute('aria-expanded','false');}function render(){if(view==='home')return home();if(view.startsWith('lesson'))return lesson(+view.slice(6));if(view==='lab')return lab();if(view==='waves')return waves();if(view==='quiz')return quiz();if(view==='checklist')return checklistPage();if(view==='sources')return sources();return home()}function home(){return `<div class="content"><div class="hero"><div><div class="eyebrow">A PRACTICAL AZURE ASSESSMENT COURSE</div><h1>Understand the estate before you move it.</h1><p class="lead">Learn Matilda Cloud through a hospital move, a real-world style case, and decisions you can practice. Start with the first lesson or jump into the labs.</p><button class="primary" data-go="lesson0">Start learning ↗</button><button class="secondary" data-go="lab">Try the dependency lab</button></div><img src="./assets/hospital.webp" alt="Illustration of connected hospital departments and a second building, used as a migration analogy"></div><div class="stats"><div class="stat"><strong>8</strong><span>guided lessons</span></div><div class="stat"><strong>2</strong><span>interactive assessment labs</span></div><div class="stat"><strong>1</strong><span>client-ready checklist</span></div></div><div class="reading"><h2>The running case</h2><p>Northstar Health runs a customer portal on VMware. Its team has a partial CMDB, a shared SQL platform, an identity service and an external payment API. In each lesson, you turn fragmented technical evidence into a more defensible Azure migration plan.</p><div class="diagram"><div class="flow"><span>Web01 + Web02</span><b>→</b><span>App01</span><b>→</b><span>SQL01</span></div><figcaption>Illustrative case. The full dependency boundary also includes shared identity and an external payment API.</figcaption></div><h2>How to use this site</h2><p>Read a lesson, try its field exercise, then use the labs to test grouping and wave logic. The quiz checks core ideas. Your lesson progress and checklist choices stay in this browser.</p><div class="callout warn"><strong>Product boundary</strong><p>These are architect-led tutorials, not a simulation of Matilda’s actual interface. Screens, permissions and outputs vary by licensed product and environment. Use the linked vendor docs for deployment steps.</p></div></div></div>`}
function lesson(i){let l=lessons[i];return `<div class="content reading"><div class="eyebrow">LESSON ${String(i+1).padStart(2,'0')} / 08 · ${l.time}</div><h1>${esc(l.title)}</h1><p class="intro">${esc(l.intro)}</p><div class="analogy"><strong>Picture it</strong><p>${esc(l.analogy)}</p></div>${i===3?`<img class="feature-image" src="./assets/dependency.webp" alt="Editorial illustration of an application system and its connected services">`:''}${l.sections.map(([h,p])=>`<section><h2>${esc(h)}</h2><p>${esc(p)}</p></section>`).join('')}<div class="exercise"><strong>Try it on a client engagement</strong><p>${esc(l.exercise)}</p></div><div class="callout"><strong>Key decision</strong><p>${esc(l.takeaway)}</p></div><div class="lessonbar"><button class="primary" data-complete="${i}">${saved.has(i)?'Completed ✓':'Mark lesson complete'}</button>${i<7?`<button class="secondary" data-go="lesson${i+1}">Next lesson →</button>`:`<button class="secondary" data-go="lab">Go to the lab →</button>`}</div><p class="small">Matilda capabilities are summarized from vendor documentation. Azure Migrate functionality is linked under Sources & limits.</p></div>`}
function lab(){const nodes=[['WEB01','Portal web tier'],['WEB02','Portal web tier'],['APP01','Business logic'],['SQL01','Portal data, shared platform'],['AD01','Shared identity'],['PAY-API','External payments'],['FILE01','Unrelated file service'],['HR01','HR application']];return `<div class="content reading"><div class="eyebrow">INTERACTIVE LAB · APPLICATION GROUPING</div><h1>Find the portal boundary</h1><p class="intro">Northstar’s CMDB has eight entries. Select the four core components that form the customer portal. Then examine what stays outside the group but still needs a dependency review.</p><div class="appgrid">${nodes.map(([n,d])=>`<button class="node ${selectedNodes.has(n)?'selected':''}" data-node="${n}" aria-pressed="${selectedNodes.has(n)}"><strong>${n}</strong><small>${d}</small></button>`).join('')}</div><button class="primary" id="checkNodes">Check grouping</button><button class="secondary" id="resetNodes">Reset</button><div id="labFeedback" class="feedback" role="status">Select the portal’s core web, app and data components.</div><h2>Architect review</h2><p>AD01 is a shared identity dependency. PAY-API is an external integration. Both belong in the application map and cutover plan, although neither is one of the four core portal servers. SQL01 is shared, so coordinate its migration with other consumers.</p><div class="diagram"><div class="flow"><span>WEB01 + WEB02</span><b>→</b><span>APP01</span><b>→</b><span>SQL01</span></div><div class="flow" style="margin-top:15px"><span>Shared AD01</span><b>↗</b><span>Portal</span><b>↗</b><span>External PAY-API</span></div><figcaption>Illustrative dependency graph, not a Matilda screenshot or automatically verified topology.</figcaption></div><div class="exercise"><strong>Workshop prompt</strong><p>Ask who else uses SQL01, how the payment API is reached, and whether the portal’s login can tolerate temporary cross-site latency.</p></div></div>`}
function waves(){return `<div class="content reading"><div class="eyebrow">INTERACTIVE LAB · WAVE PLANNING</div><h1>Choose the first wave</h1><p class="intro">The target landing zone exists. Choose a first migration group for a pilot, then read the trade-off.</p><div class="panel"><table class="case-table"><thead><tr><th>Group</th><th>Technical picture</th><th>Business constraint</th></tr></thead><tbody><tr><td>A · Dev portal</td><td>2 VMs, isolated test data</td><td>Owner available for testing</td></tr><tr><td>B · Production portal</td><td>4 core VMs + shared SQL</td><td>Payment and identity integration</td></tr><tr><td>C · Payroll</td><td>3 VMs, batch dependencies</td><td>Month-end blackout</td></tr></tbody></table></div><button class="choice" data-wave="A">A · Dev portal</button><button class="choice" data-wave="B">B · Production portal</button><button class="choice" data-wave="C">C · Payroll</button><div id="waveFeedback" class="feedback" role="status">Select a group to see the rationale.</div><h2>What this pilot should prove</h2><p>Test network paths, identity, monitoring, backups, deployment, application validation and rollback. A pilot that only proves a VM can boot in Azure has limited value. Then plan the production portal only after mapping every consumer of the shared SQL platform and agreeing the payment API path.</p><div class="callout warn"><strong>Decision nuance</strong><p>Group A is a reasonable first pilot under these assumptions. A different choice can be valid if there is hidden coupling, an owner constraint or a better low-risk learning target. Record why.</p></div></div>`}
function quiz(){if(qIndex>=questions.length)return `<div class="content reading"><div class="eyebrow">KNOWLEDGE CHECK</div><h1>${score} / ${questions.length} correct</h1><p class="intro">Review anything you missed, then apply the checklist to a real engagement.</p><button class="primary" id="restartQuiz">Try again</button><button class="secondary" data-go="checklist">Open checklist</button></div>`;let q=questions[qIndex];return `<div class="content reading"><div class="eyebrow">QUESTION ${qIndex+1} / ${questions.length}</div><h1>${esc(q.q)}</h1><div class="progressbar"><i style="width:${qIndex/questions.length*100}%"></i></div>${q.choices.map((c,i)=>`<button class="choice" data-answer="${i}" ${answered?'disabled':''}>${esc(c)}</button>`).join('')}<div id="quizFeedback" class="feedback" role="status">Choose one answer.</div><button class="primary" id="nextQ" style="display:none">${qIndex===questions.length-1?'See result':'Next question →'}</button></div>`}
function checklistPage(){let checked=new Set(JSON.parse(localStorage.getItem('matildaChecks')||'[]'));return `<div class="content reading"><div class="eyebrow">FIELD TOOL</div><h1>Assessment readiness checklist</h1><p class="intro">Use this as a workshop aid before you publish a recommendation. Selections stay in this browser and do not become a client record.</p><div class="panel">${checklist.map((x,i)=>`<label class="checkrow"><input type="checkbox" data-check="${i}" ${checked.has(i)?'checked':''}><span>${esc(x)}</span></label>`).join('')}</div><p id="checkCount" class="small">${checked.size} of ${checklist.length} reviewed</p><button class="secondary" id="clearChecks">Clear choices</button><div class="callout warn"><strong>Publication gate</strong><p>Any unchecked item needs an owner, a resolution date or a clearly stated limitation in the final assessment.</p></div></div>`}
function sources(){return `<div class="content reading" id="sources"><div class="eyebrow">SOURCES & LIMITS</div><h1>Verify against the live product</h1><p class="intro">This site teaches an assessment method. It does not claim to reproduce Matilda’s current screens or every licensed feature.</p><h2>Primary references</h2><ul class="source-list"><li><a target="_blank" rel="noopener" href="https://docs.matildacloud.com/">Matilda Cloud documentation</a> for supported discovery methods, prerequisites, permissions and workflows.</li><li><a target="_blank" rel="noopener" href="https://www.matildacloud.com/products">Matilda Cloud product overview</a> for Discover, RAPID Assessment and migration capabilities.</li><li><a target="_blank" rel="noopener" href="https://learn.microsoft.com/en-us/azure/migrate/concepts-dependency-visualization">Microsoft Learn: dependency analysis</a> for current Azure Migrate options and limits.</li><li><a target="_blank" rel="noopener" href="https://learn.microsoft.com/en-us/azure/migrate/concepts-assessment-overview">Microsoft Learn: assessment overview</a> for Azure target assessments.</li></ul><h2>How the examples are framed</h2><p>Northstar Health, server names, topology, wave choices and numeric examples are fictional teaching cases. The hospital imagery is an analogy. Product availability, collection detail and pricing vary by environment and agreement.</p><div class="callout"><strong>Before a customer workshop</strong><p>Check the exact Matilda module and version, current prerequisites, security documentation, licensed capabilities and Azure Migrate experience your team will use.</p></div></div>`}
document.addEventListener('click',e=>{let t=e.target.closest('[data-go],[data-view],[data-complete],[data-node],[data-wave],[data-answer],#checkNodes,#resetNodes,#restartQuiz,#nextQ,#clearChecks,#menuBtn,#sourceLink');if(!t)return;if(t.id==='menuBtn'){let open=$('.sidebar').classList.toggle('open');t.setAttribute('aria-expanded',String(open));return}if(t.id==='sourceLink'){e.preventDefault();setView('sources');return}if(t.dataset.go||t.dataset.view){setView(t.dataset.go||t.dataset.view);return}if(t.dataset.complete!==undefined){saved.add(+t.dataset.complete);localStorage.setItem('matildaDone',JSON.stringify([...saved]));setView(view);return}if(t.dataset.node){let n=t.dataset.node;selectedNodes.has(n)?selectedNodes.delete(n):selectedNodes.add(n);t.classList.toggle('selected');t.setAttribute('aria-pressed',selectedNodes.has(n));return}if(t.id==='resetNodes'){selectedNodes.clear();setView('lab');return}if(t.id==='checkNodes'){let core=['WEB01','WEB02','APP01','SQL01'];let good=selectedNodes.size===4&&core.every(n=>selectedNodes.has(n));$('#labFeedback').innerHTML=good?'<strong>Correct core group.</strong> Now document AD01 and PAY-API as dependencies, and flag SQL01 as a shared platform.':'Review the core group. It contains the two web servers, the app server and SQL01. Shared identity and external payments still need mapping.';return}if(t.dataset.wave){document.querySelectorAll('[data-wave]').forEach(b=>b.classList.remove('selected'));t.classList.add('selected');$('#waveFeedback').textContent=t.dataset.wave==='A'?'A is the best pilot here: isolated test data, an available owner and enough Azure integration checks to learn before production.':t.dataset.wave==='B'?'B carries shared SQL, payment and identity dependencies. Validate these before production cutover; it is a risky first pilot.':'C conflicts with the month-end blackout and batch paths. Schedule it after dependency validation and a suitable business window.';return}if(t.dataset.answer!==undefined&&!answered){let a=+t.dataset.answer,q=questions[qIndex];answered=true;if(a===q.answer)score++;document.querySelectorAll('[data-answer]').forEach((b,i)=>{b.disabled=true;b.classList.add(i===q.answer?'correct':i===a?'wrong':'')});$('#quizFeedback').textContent=(a===q.answer?'Correct. ':'Revisit this. ')+q.why;$('#nextQ').style.display='inline-flex';return}if(t.id==='nextQ'){qIndex++;answered=false;setView('quiz');return}if(t.id==='restartQuiz'){qIndex=0;score=0;answered=false;setView('quiz');return}if(t.id==='clearChecks'){localStorage.removeItem('matildaChecks');setView('checklist')}});document.addEventListener('change',e=>{if(e.target.dataset.check===undefined)return;let set=new Set(JSON.parse(localStorage.getItem('matildaChecks')||'[]'));let i=+e.target.dataset.check;e.target.checked?set.add(i):set.delete(i);localStorage.setItem('matildaChecks',JSON.stringify([...set]));$('#checkCount').textContent=`${set.size} of ${checklist.length} reviewed`});nav();setView('home');
