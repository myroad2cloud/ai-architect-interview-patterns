/* Matilda Learning Lab: sourced field workflows and reusable exercises. */
const docs = {
  platform: 'https://docs.matildacloud.com/docs/es-mx/platform-capabilities-arc-update',
  windows: 'https://docs.matildacloud.com/docs/es-mx/windows-service-account-setup',
  access: 'https://docs.matildacloud.com/docs/es-mx/pre-requisites-and-access-arc-update',
  products: 'https://www.matildacloud.com/products',
  migrate: 'https://learn.microsoft.com/en-us/azure/migrate/concepts-dependency-visualization',
  sizing: 'https://learn.microsoft.com/en-us/azure/migrate/concepts-assessment-overview',
  precheck: 'https://docs.matildacloud.com/docs/es-mx/precheck-utility-user-guide'
};
lessons.push({
  title:'A first Matilda assessment',time:'18 min',intro:'Follow a small Windows pilot from access review to a report. The menu path below is documented for Windows discovery; confirm it in your licensed Matilda environment before use.',
  analogy:'A hospital move starts with one ward. First prove the keys, corridors and equipment list, then scale the process across the building.',
  sections:[
    ['1. Agree the pilot','Choose a handful of Windows servers with a known owner and a simple application. Record hostnames, source network, scope exclusions and expected inventory. Obtain approval for the collection window and account access.'],
    ['2. Prepare the collector and access','Use the deployment model supplied for your Matilda engagement. The documented Windows method uses a Matilda VM with access to targets. The service account requires local Administrators and Remote Management Users membership for complete discovery. Treat this as privileged access and scope it tightly.'],
    ['3. Validate network paths','The Windows guide lists WinRM 5985/5986 and SMB 445 from the Matilda VM to targets. Prefer WinRM over HTTPS 5986 with a valid certificate where supported. Restrict the firewall rule to the collector address. The wider prerequisite guide lists SSH 22 for Linux and database ports only when deep database discovery is in scope.'],
    ['4. Configure Windows discovery','The documented path is Settings → Discovery Credentials. Add the approved WinRM/WMI credential, set the target systems or subnets, then initiate the scan. Menu labels and role access may vary by version; use the vendor guide beside the portal.'],
    ['5. Pre-check and reconcile','With customer approval, run the documented Matilda precheck utility or equivalent readiness validation. Its report checks reachability, authentication and privileges per host. Then check pre-check, discovery overall and deep discovery status and export inventory. Compare expected and discovered assets. Classify missing hosts as access, network, powered-off, exclusion or stale CMDB entries. Do not advance with unexplained gaps.'],
    ['6. Review the assessment','The platform documents interim and detailed assessment reporting, plus XLSX assessment summaries and PPT/PDF executive summaries. Inspect the report’s target, sizing assumptions, unsupported assets and missing data. Have the application owner validate the grouping and dependencies before approving a wave.']
  ],exercise:'Run a pilot only in an authorized environment. Record each prerequisite, its owner and proof of validation. If you do not have Matilda access, use the fictional Northstar case to rehearse the reconciliation and decision log.',takeaway:'A successful scan is a collection milestone. An approved assessment needs reconciled scope, owner validation and documented assumptions.',sourceLinks:[['Windows discovery guide',docs.windows],['Platform capabilities and reports',docs.platform],['Access prerequisites',docs.access],['Precheck utility guide',docs.precheck]]
});
lessons.push({
  title:'RAPID and Discover',time:'8 min',intro:'Choose the depth of assessment to match the decision. Confirm the module licensed for the engagement.',
  analogy:'An airport security scan quickly finds obvious issues. A specialist inspection maps how a complex machine works before it is moved.',
  sections:[
    ['RAPID Assessment','Matilda positions RAPID around environment inventory, cloud cost projections, EOS/EOSL and compliance risk insights, and executive-level reports. Use it for a directional opportunity or early business case, with clear confidence limits.'],
    ['Matilda Discover','Discover is positioned around application, infrastructure and service dependency mapping, workload architecture, resource forecasting, modernization analysis and automated wave planning. Deeper outputs depend on collection access and owner validation.'],
    ['Decision rule','Ask what the customer must decide now. A portfolio screening may need RAPID. A migration wave or modernization commitment needs application-level evidence and a deeper Discover workflow. Check the contract, licensed capabilities, supported sources and expected report examples before writing the SOW.']
  ],exercise:'Write two separate deliverable lists: one for a two-week directional assessment and one for detailed application migration planning. Mark what depends on deep discovery and workshops.',takeaway:'Product names describe advertised capability. Your SOW must specify the actual evidence and deliverables.',sourceLinks:[['Matilda product overview',docs.products],['Platform capabilities',docs.platform]]
});
questions.push({q:'Which Windows access does Matilda’s documented agentless discovery method call for?',choices:['No credentials because it is agentless','A scoped account in local Administrators and Remote Management Users','Domain Admin for every machine'],answer:1,why:'The Windows guide lists local Administrator and Remote Management Users membership. Treat the access as privileged and constrain it.'});
questions.push({q:'What is a safe use of a RAPID Assessment output?',choices:['A directional portfolio view with stated confidence','Proof that every application dependency is validated','A signed cutover plan for a shared SQL cluster'],answer:0,why:'RAPID is positioned for inventory, cost and risk insights. Detailed wave decisions require deeper evidence.'});
const originalHome=home, originalLesson=lesson, originalSources=sources;
home=function(){let html=originalHome();return html.replace('<strong>8</strong><span>guided lessons</span>','<strong>10</strong><span>guided lessons</span>').replace('two original illustrations','two original illustrations').replace('Read a lesson, try its field exercise','Follow the sourced pilot walkthrough, read a lesson, try its field exercise').replace('<h2>How to use this site</h2>','<h2>How to use this site</h2><p><button class="secondary" data-go="lesson8">Open the Matilda pilot walkthrough →</button><button class="secondary" data-go="security">Review access and ports →</button></p>');};
lesson=function(i){let l=lessons[i];let source=l.sourceLinks?.length?`<div class="panel"><h3>Verify these steps</h3><ul class="source-list">${l.sourceLinks.map(([label,url])=>`<li><a href="${url}" target="_blank" rel="noopener noreferrer">${esc(label)} ↗</a></li>`).join('')}</ul></div>`:'';let visual=i===1?'<img class="lesson-diagram" src="./assets/discovery-flow.svg" alt="Matilda collector connects to scoped source systems; output is checked against CMDB and owner input">':i===3?'<img class="lesson-diagram" src="./assets/application-map.svg" alt="Portal web tier reaches app and SQL, with shared identity and external payment dependencies">':'';return `<div class="content reading"><div class="eyebrow">LESSON ${String(i+1).padStart(2,'0')} / ${lessons.length} · ${l.time}</div><h1>${esc(l.title)}</h1><p class="intro">${esc(l.intro)}</p><div class="analogy"><strong>Picture it</strong><p>${esc(l.analogy)}</p></div>${visual}${l.sections.map(([h,p])=>`<section><h2>${esc(h)}</h2><p>${esc(p)}</p></section>`).join('')}<div class="exercise"><strong>Try it on a client engagement</strong><p>${esc(l.exercise)}</p></div><div class="callout"><strong>Key decision</strong><p>${esc(l.takeaway)}</p></div>${source}<div class="lessonbar"><button class="primary" data-complete="${i}">${saved.has(i)?'Completed ✓':'Mark lesson complete'}</button>${i<lessons.length-1?`<button class="secondary" data-go="lesson${i+1}">Next lesson →</button>`:`<button class="secondary" data-go="lab">Go to the lab →</button>`}</div><p class="small">Fictional case data is for practice. Product steps link to the vendor source where available.</p></div>`;};
const model={owner:'',criticality:'',shared:'',external:'',evidence:'',unknown:'',rationale:'',wave:'',cutover:''};
const originalLab=lab;
lab=function(){return originalLab().replace('<h2>Architect review</h2>','<h2>Build the application record</h2><p>Complete this field note after checking the group. The export stays on your device.</p><div class="formgrid"><label>Application owner<input data-model="owner" placeholder="Example: portal product owner" value="'+esc(model.owner)+'"></label><label>Business criticality<select data-model="criticality"><option value="">Choose level</option><option '+(model.criticality==='High'?'selected':'')+'>High</option><option '+(model.criticality==='Medium'?'selected':'')+'>Medium</option><option '+(model.criticality==='Low'?'selected':'')+'>Low</option></select></label><label>Shared platform dependency<input data-model="shared" placeholder="Example: SQL01 serves HR too" value="'+esc(model.shared)+'"></label><label>External dependency<input data-model="external" placeholder="Example: payment API over private link" value="'+esc(model.external)+'"></label><label class="span2">Evidence and observation window<textarea data-model="evidence" placeholder="Traffic window, CMDB row, owner workshop">'+esc(model.evidence)+'</textarea></label><label class="span2">Unknowns to validate<textarea data-model="unknown" placeholder="Who else uses SQL01? Month-end batch?">'+esc(model.unknown)+'</textarea></label></div><button class="primary" data-export="application">Download application record</button><h2>Architect review</h2>');};
waves=function(){return `<div class="content reading"><div class="eyebrow">INTERACTIVE LAB · WAVE PLANNING</div><h1>Build a pilot decision</h1><p class="intro">Compare three groups and record a defensible wave recommendation. This is a fictional case, so state assumptions.</p><div class="panel"><table class="case-table"><thead><tr><th>Group</th><th>Technical picture</th><th>Business constraint</th></tr></thead><tbody><tr><td>A · Dev portal</td><td>2 VMs, isolated test data</td><td>Owner available for testing</td></tr><tr><td>B · Production portal</td><td>4 core VMs + shared SQL</td><td>Payment and identity integration</td></tr><tr><td>C · Payroll</td><td>3 VMs, batch dependencies</td><td>Month-end blackout</td></tr></tbody></table></div><fieldset class="wave-options"><legend>Which group should be the first pilot?</legend>${['A','B','C'].map(x=>`<label class="choice"><input type="radio" name="pilot" value="${x}" ${model.wave===x?'checked':''}> Group ${x}</label>`).join('')}</fieldset><div class="formgrid"><label class="span2">Why this group first?<textarea data-model="rationale" placeholder="Risk, learning objective and dependencies">${esc(model.rationale)}</textarea></label><label class="span2">Cutover proof and rollback trigger<textarea data-model="cutover" placeholder="Smoke tests, identity, monitoring, rollback threshold">${esc(model.cutover)}</textarea></label></div><button class="primary" data-review-wave>Review decision</button><button class="secondary" data-export="wave">Download wave decision</button><div id="waveFeedback" class="feedback" role="status">Select a pilot and write your rationale.</div><h2>What this pilot should prove</h2><p>Test network paths, identity, monitoring, backups, deployment, application validation and rollback. A pilot that only proves a VM can boot in Azure has limited value. Plan the production portal after mapping every consumer of shared SQL01 and agreeing the payment path.</p></div>`;};
function security(){return `<div class="content reading"><div class="eyebrow">SECURITY & NETWORK ARCHITECTURE</div><h1>Discovery access matrix</h1><p class="intro">Use this as a review starter, then confirm the exact deployment model, version, ports and permissions in your Matilda engagement. Do not apply broad rules without an approved scope.</p><img class="lesson-diagram" src="./assets/discovery-flow.svg" alt="Collector network paths and validation flow"><div class="tablewrap"><table class="case-table"><thead><tr><th>Target</th><th>Documented access</th><th>Network path</th><th>Control to verify</th></tr></thead><tbody><tr><td>Windows</td><td>Local Administrators and Remote Management Users for documented deep discovery</td><td>Collector to target: WinRM 5985/5986, SMB 445</td><td>Prefer HTTPS 5986; source-restrict rules; vault and rotate credentials</td></tr><tr><td>Linux/Unix</td><td>SSH account with approved sudo/pbrun/dzdo permissions</td><td>Collector to target: SSH 22</td><td>Scope commands, log activity and review configuration-file access</td></tr><tr><td>SQL Server and other databases</td><td>Read-only metadata account per engine</td><td>Engine port, such as SQL Server 1433; custom ports vary</td><td>Confirm metadata versus application data, retention and report access</td></tr><tr><td>Matilda service</td><td>Licensed tenant, collector and credential vault</td><td>Approved outbound SaaS path</td><td>Validate residency, encryption, proxy, DNS and offboarding</td></tr></tbody></table></div><div class="callout warn"><strong>Privileged access needs scrutiny</strong><p>The Windows guide names local Administrator membership even while describing read-only collection. Permission level and tool behavior are different questions. Have security approve the account, collector source, firewall scope and removal plan. Do not claim WinRM on port 5985 is encrypted merely because the guide calls the overall path secure.</p></div><h2>Preflight evidence</h2><ol><li>Capture the approved target list and collector IP.</li><li>Test DNS, port reachability and authentication against a pilot host.</li><li>Review scan logs for denied paths and partial data.</li><li>Record credential rotation and removal after the assessment.</li></ol><div class="panel"><h3>Vendor references</h3><ul class="source-list"><li><a href="${docs.windows}" target="_blank" rel="noopener noreferrer">Windows service account setup ↗</a></li><li><a href="${docs.access}" target="_blank" rel="noopener noreferrer">Agentless prerequisites and port matrix ↗</a></li></ul></div></div>`;}
sources=function(){let s=originalSources();return s.replace('<h2>How the examples are framed</h2>',`<h2>Product-specific guides</h2><ul class="source-list"><li><a href="${docs.windows}" target="_blank" rel="noopener noreferrer">Matilda Windows discovery access and menu path</a></li><li><a href="${docs.access}" target="_blank" rel="noopener noreferrer">Matilda service account and network prerequisites</a></li><li><a href="${docs.platform}" target="_blank" rel="noopener noreferrer">Matilda assessment capabilities and report formats</a></li><li><a href="${docs.precheck}" target="_blank" rel="noopener noreferrer">Matilda precheck utility guide</a></li></ul><h2>How the examples are framed</h2>`);};
const originalRender=render;render=function(){if(view==='security')return security();return originalRender();};
nav=function(){let h='<button class="navbtn '+(view==='home'?'active':'')+'" data-view="home">⌂ <span>Overview</span></button>';lessons.forEach((l,i)=>h+=`<button class="navbtn ${view==='lesson'+i?'active':''}" data-view="lesson${i}"><span class="num">${saved.has(i)?'✓':String(i+1).padStart(2,'0')}</span><span>${esc(l.title)}</span></button>`);$('#lessonNav').innerHTML=h;document.querySelectorAll('.navbtn[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===view));$('#progressText').textContent=`${saved.size} / ${lessons.length}`;$('#progressBar').style.width=(saved.size/lessons.length*100)+'%';};
const validView=v=>v==='home'||v==='lab'||v==='waves'||v==='quiz'||v==='checklist'||v==='sources'||v==='security'||(/^lesson\d+$/.test(v)&&+v.slice(6)<lessons.length);
setView=function(v,fromHistory=false){if(!validView(v))v='home';view=v;nav();$('#content').innerHTML=render();$('#content').focus();window.scrollTo(0,0);closeMenu(false);document.title=v==='home'?'Matilda Learning Lab | Azure assessment field guide':(v.startsWith('lesson')?lessons[+v.slice(6)].title:v[0].toUpperCase()+v.slice(1))+' | Matilda Learning Lab';if(!fromHistory&&location.hash.slice(1)!==v)history.pushState({view:v},'',`#${v}`);};
function closeMenu(restoreFocus){$('.sidebar').classList.remove('open');$('#menuBtn').setAttribute('aria-expanded','false');$('#menuBackdrop').hidden=true;if(restoreFocus)$('#menuBtn').focus();}
const backdrop=document.createElement('button');backdrop.id='menuBackdrop';backdrop.className='menu-backdrop';backdrop.hidden=true;backdrop.setAttribute('aria-label','Close navigation');document.querySelector('.shell').append(backdrop);
const close=document.createElement('button');close.id='closeMenu';close.className='close-menu';close.setAttribute('aria-label','Close navigation');close.textContent='×';$('.brand').append(close);
const navContainer=$('.sidebar nav:last-of-type');navContainer.insertAdjacentHTML('beforeend','<button class="navbtn" data-view="security">◇ <span>Access & security</span></button>');
document.addEventListener('click',e=>{if(e.target.id==='menuBtn'){backdrop.hidden=!$('.sidebar').classList.contains('open');if(!backdrop.hidden)close.focus();}if(e.target.id==='menuBackdrop'||e.target.id==='closeMenu'){closeMenu(true);return}if(e.target.dataset.export){let type=e.target.dataset.export;let fields=type==='application'?`# Northstar customer portal · application record\nCore servers: ${[...selectedNodes].sort().join(', ')||'Not selected'}\nOwner: ${model.owner||'To confirm'}\nCriticality: ${model.criticality||'To confirm'}\nShared platform: ${model.shared||'To confirm'}\nExternal integration: ${model.external||'To confirm'}\nEvidence: ${model.evidence||'To confirm'}\nUnknowns: ${model.unknown||'To confirm'}\n`:`# Northstar pilot · wave decision\nSelected group: ${model.wave||'To decide'}\nRationale: ${model.rationale||'To confirm'}\nCutover proof and rollback: ${model.cutover||'To confirm'}\nAssumptions: Fictional training case; validate with owners.\n`;let blob=new Blob([fields],{type:'text/plain;charset=utf-8'});let url=URL.createObjectURL(blob);let a=document.createElement('a');a.href=url;a.download=type==='application'?'application-record.txt':'wave-decision.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);return}if(e.target.dataset.reviewWave!==undefined){let f=$('#waveFeedback');f.textContent=!model.wave?'Choose a group first.':!model.rationale.trim()?'Write the reason, dependencies and learning goal.':model.wave==='A'?'Reasonable pilot if its data is isolated and the owner can validate. Confirm its network, identity and rollback tests.':model.wave==='B'?'High-risk first pilot: shared SQL and external integrations need deeper validation. Explain why this is still justified.':'Payroll has a month-end blackout and batch dependencies. Choose an approved window and prove those paths.';return}});
document.addEventListener('change',e=>{if(e.target.name==='pilot')model.wave=e.target.value;if(e.target.dataset.model)model[e.target.dataset.model]=e.target.value});document.addEventListener('input',e=>{if(e.target.dataset.model)model[e.target.dataset.model]=e.target.value});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&$('.sidebar').classList.contains('open'))closeMenu(true)});window.addEventListener('popstate',()=>setView(location.hash.slice(1)||'home',true));window.addEventListener('hashchange',()=>{let v=location.hash.slice(1);if(v!==view)setView(v,true)});
setView(location.hash.slice(1)||'home',true);
